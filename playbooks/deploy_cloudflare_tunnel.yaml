- name: Deploy Cloudflare tunnel
  hosts: cluster
  vars_files:
    - ../vars/vault.yaml
  roles:
    - role: deploy_cloudflare_tunnel
      vars:
        subdomain: ansible-roles
        tunnel_name: ansible-roles
